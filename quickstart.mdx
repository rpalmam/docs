---
title: Quickstart
description: Make your first API call in under 2 minutes.
---

## 1. Get your API key

Purchase a module at [enrichlatam.com](https://enrichlatam.com). Your API key will be sent to your email immediately after checkout.

Your key looks like this:

```
sk_a1b2c3d4e5f6...
```

## 2. Make your first request

Use your key in the `X-API-Key` header. Here's a KYB lookup for a Chilean company by RUT:

<CodeGroup>

```bash cURL
curl -X POST https://api.enrichlatam.com/v1/kyb \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"identifier": "76354771-K", "country": "CL"}'
```

```python Python
import requests

response = requests.post(
    "https://api.enrichlatam.com/v1/kyb",
    headers={"X-API-Key": "YOUR_API_KEY"},
    json={"identifier": "76354771-K", "country": "CL"}
)
print(response.json())
```

```javascript Node.js
const response = await fetch("https://api.enrichlatam.com/v1/kyb", {
  method: "POST",
  headers: {
    "X-API-Key": "YOUR_API_KEY",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({ identifier: "76354771-K", country: "CL" })
});
const data = await response.json();
console.log(data);
```

</CodeGroup>

## 3. Read the response

```json
{
  "rut": "76354771-K",
  "razon_social": "EMPRESA EJEMPLO SpA",
  "activo": true,
  "inicio_actividades": "2018-03-15",
  "actividad_economica": "Actividades de consultoría de gestión empresarial",
  "direccion": "Av. Providencia 1234, Providencia, Región Metropolitana",
  "source": "sii",
  "timestamp": "2026-04-05T10:00:00Z"
}
```

<Check>You're live. The `activo: true` field means the company is registered and active with the tax authority.</Check>

## Next steps

<Columns cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Learn how API keys and module access work.
  </Card>
  <Card title="KYB Module" icon="shield-check" href="/modules/kyb">
    Full KYB reference with all fields and error codes.
  </Card>
  <Card title="Enrich Module" icon="building" href="/modules/enrich">
    Company enrichment with contact, social, and firmographic data.
  </Card>
  <Card title="Tenders Module" icon="file-contract" href="/modules/tenders">
    Query public procurement tenders.
  </Card>
</Columns>
