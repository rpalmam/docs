---
title: "POST /v1/sanctions"
description: "Check if a company appears on international sanctions lists (OFAC SDN, UN Consolidated)."
---

<ParamField header="X-API-Key" type="string" required>
  Your API key. Requires the `sanctions` module.
</ParamField>

## Request body

<ParamField body="identifier" type="string" required>
  Company tax identifier. RUT for Chile (e.g. `76354771-K`) or CNPJ for Brazil (e.g. `12.345.678/0001-90`). Formatting is optional.
</ParamField>

<ParamField body="country" type="string" required>
  ISO 3166-1 alpha-2 country code. Supported: `CL`, `BR`.
</ParamField>

## Response

<ResponseField name="identifier" type="string">
  The queried tax identifier, as provided.
</ResponseField>

<ResponseField name="country" type="string">
  Country code.
</ResponseField>

<ResponseField name="is_sanctioned" type="boolean">
  `true` only if the exact identifier was found in at least one sanctions list. `false` otherwise — no fuzzy matching.
</ResponseField>

<ResponseField name="matches" type="array">
  List of match objects. Empty array if `is_sanctioned` is `false`.
</ResponseField>

<ResponseField name="matches[].list_name" type="string">
  Name of the list that contains this match: `OFAC_SDN` or `UN_CONSOLIDATED`.
</ResponseField>

<ResponseField name="matches[].entry_name" type="string">
  Name of the sanctioned entity as it appears in the list.
</ResponseField>

<ResponseField name="matches[].programs" type="array">
  Sanctions programs associated with this entry (e.g. `["SDNTK"]`, `["Al-Qaida"]`).
</ResponseField>

<ResponseField name="matches[].matched_id_type" type="string">
  Type of identifier that matched (e.g. `"RUT"`, `"CNPJ"`, `"TAX ID NO."`).
</ResponseField>

<ResponseField name="matches[].matched_id_value" type="string">
  Original identifier value as it appears in the sanctions list.
</ResponseField>

<ResponseField name="matches[].uid" type="string">
  List-specific unique entry identifier.
</ResponseField>

<ResponseField name="lists_checked" type="array">
  Names of all lists that were checked in this request.
</ResponseField>

<ResponseField name="lists_updated_at" type="object">
  ISO 8601 datetime of the last successful refresh per list. `null` if a list failed to load.
</ResponseField>

<ResponseField name="checked_at" type="string">
  ISO 8601 datetime of this specific check.
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.enrichlatam.com/v1/sanctions \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"identifier": "76354771-K", "country": "CL"}'
```

```python Python
import requests

r = requests.post(
    "https://api.enrichlatam.com/v1/sanctions",
    headers={"X-API-Key": "YOUR_API_KEY"},
    json={"identifier": "76354771-K", "country": "CL"},
)
print(r.json())
```

```javascript Node.js
const res = await fetch("https://api.enrichlatam.com/v1/sanctions", {
  method: "POST",
  headers: {
    "X-API-Key": "YOUR_API_KEY",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({ identifier: "76354771-K", country: "CL" }),
});
console.log(await res.json());
```

</RequestExample>

<ResponseExample>

```json 200 Not sanctioned
{
  "identifier": "76354771-K",
  "country": "CL",
  "is_sanctioned": false,
  "matches": [],
  "lists_checked": ["OFAC_SDN", "UN_CONSOLIDATED"],
  "lists_updated_at": {
    "OFAC_SDN": "2026-04-14T08:00:00+00:00",
    "UN_CONSOLIDATED": "2026-04-14T08:00:05+00:00"
  },
  "checked_at": "2026-04-14T12:00:00+00:00"
}
```

```json 200 Sanctioned
{
  "identifier": "76999999-K",
  "country": "CL",
  "is_sanctioned": true,
  "matches": [
    {
      "list_name": "OFAC_SDN",
      "entry_name": "EMPRESA EJEMPLO LTDA",
      "programs": ["SDNTK"],
      "matched_id_type": "RUT",
      "matched_id_value": "76.999.999-K",
      "uid": "12345"
    }
  ],
  "lists_checked": ["OFAC_SDN", "UN_CONSOLIDATED"],
  "lists_updated_at": {
    "OFAC_SDN": "2026-04-14T08:00:00+00:00",
    "UN_CONSOLIDATED": "2026-04-14T08:00:05+00:00"
  },
  "checked_at": "2026-04-14T12:00:00+00:00"
}
```

```json 422 Personal RUT blocked
{
  "detail": "Personal RUTs are not supported. This endpoint is for business entities only."
}
```

```json 403 Module not in plan
{
  "detail": "Your plan does not include the 'sanctions' module. Upgrade at enrichlatam.com."
}
```

</ResponseExample>
