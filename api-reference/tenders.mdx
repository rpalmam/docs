---
title: "POST /v1/tenders"
description: "Get public tenders for a company by tax ID or domain."
---

<ParamField header="X-API-Key" type="string" required>
  Your API key. Requires the `tenders` module.
</ParamField>

## Request body

<ParamField body="identifier" type="string">
  Company tax ID — RUT (Chile, e.g. `77.261.280-K`), CNPJ (Brazil, e.g. `28.591.276/0001-00`), or NIT (Colombia, e.g. `900123456-1`). One of `identifier` or `domain` is required.
</ParamField>

<ParamField body="domain" type="string">
  Company domain (e.g. `empresa.com.br`). Resolved to tax ID automatically. Supported for CL and BR only. One of `identifier` or `domain` is required.
</ParamField>

<ParamField body="country" type="string" required>
  ISO 3166-1 alpha-2 country code. Supported: `CL`, `BR`, `CO`.
</ParamField>

## Response

<ResponseField name="identifier" type="string">
  The queried tax ID (RUT, CNPJ, or NIT).
</ResponseField>

<ResponseField name="total_orders" type="integer">
  Total number of contracts returned.
</ResponseField>

<ResponseField name="orders" type="array">
  List of contract objects.

  <Expandable title="Order object fields">
    <ResponseField name="code" type="string">
      Contract or process code from the source system.
    </ResponseField>
    <ResponseField name="name" type="string">
      Contract title/description.
    </ResponseField>
    <ResponseField name="status" type="string">
      Current contract status (varies by country).
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="source" type="string">
  Data source: `mercadopublico` (CL), `pncp` (BR), `secopii` (CO).
</ResponseField>

<RequestExample>

```bash Chile
curl -X POST https://api.enrichlatam.com/v1/tenders \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"identifier": "77.261.280-K", "country": "CL"}'
```

```bash Brazil
curl -X POST https://api.enrichlatam.com/v1/tenders \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"identifier": "28.591.276/0001-00", "country": "BR"}'
```

```bash Colombia
curl -X POST https://api.enrichlatam.com/v1/tenders \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"identifier": "900123456-1", "country": "CO"}'
```

```python Python
import requests

r = requests.post(
    "https://api.enrichlatam.com/v1/tenders",
    headers={"X-API-Key": "YOUR_API_KEY"},
    json={"rut": "77.261.280-K", "country": "CL"},
)
print(r.json())
```

</RequestExample>

<ResponseExample>

```json 200 Chile
{
  "identifier": "77.261.280-K",
  "total_orders": 3,
  "orders": [
    {
      "code": "1003473-1501-SE26",
      "name": "Compra equipos computacionales",
      "status": "vigente"
    }
  ],
  "source": "mercadopublico"
}
```

```json 200 Brazil
{
  "identifier": "28.591.276/0001-00",
  "total_orders": 2,
  "orders": [
    {
      "code": "90001/2026",
      "name": "Aquisição de equipamentos de TI",
      "status": "Contrato (termo inicial)"
    }
  ],
  "source": "pncp"
}
```

```json 200 Colombia
{
  "identifier": "900123456-1",
  "total_orders": 2,
  "orders": [
    {
      "code": "CO1.BDOS.123456",
      "name": "Adquisición de equipos de cómputo",
      "status": "Celebrado"
    }
  ],
  "source": "secopii",
  "timestamp": "2026-04-13T10:00:00Z"
}
```

```json 404
{
  "detail": "No tenders found for this organization"
}
```

```json 403
{
  "detail": "Your plan does not include the 'tenders' module. Upgrade at enrichlatam.com."
}
```

</ResponseExample>
