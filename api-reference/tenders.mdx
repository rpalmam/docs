---
title: "POST /v1/tenders"
description: "Get public tenders for a company by tax ID or domain."
---

<ParamField header="X-API-Key" type="string" required>
  Your API key. Requires the `tenders` module.
</ParamField>

## Request body

<ParamField body="rut" type="string">
  Company tax ID — RUT (Chile, e.g. `77.261.280-K`) or CNPJ (Brazil, e.g. `28.591.276/0001-00`). One of `rut` or `domain` is required.
</ParamField>

<ParamField body="domain" type="string">
  Company domain (e.g. `empresa.com.br`). Resolved to tax ID automatically. One of `rut` or `domain` is required.
</ParamField>

<ParamField body="country" type="string" required>
  ISO 3166-1 alpha-2 country code. Supported: `CL`, `BR`.
</ParamField>

## Response

<ResponseField name="rut" type="string">
  The queried organization RUT.
</ResponseField>

<ResponseField name="organization" type="string">
  Organization name.
</ResponseField>

<ResponseField name="tenders" type="array">
  List of tender objects.

  <Expandable title="Tender object fields">
    <ResponseField name="id" type="string">
      Tender ID from Mercado Público.
    </ResponseField>
    <ResponseField name="name" type="string">
      Tender title/description.
    </ResponseField>
    <ResponseField name="status" type="string">
      Current tender status (e.g. `Publicada`, `Adjudicada`, `Desierta`).
    </ResponseField>
    <ResponseField name="amount" type="number">
      Estimated amount in local currency.
    </ResponseField>
    <ResponseField name="currency" type="string">
      Currency code (e.g. `CLP`).
    </ResponseField>
    <ResponseField name="published_at" type="string">
      ISO date when the tender was published.
    </ResponseField>
    <ResponseField name="closing_at" type="string">
      ISO date of tender closing deadline.
    </ResponseField>
    <ResponseField name="url" type="string">
      Link to the official tender listing.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="total" type="integer">
  Total number of tenders returned.
</ResponseField>

<ResponseField name="source" type="string">
  Data source: `mercadopublico` for Chile, `pncp` for Brazil.
</ResponseField>

<ResponseField name="timestamp" type="string">
  ISO 8601 response timestamp.
</ResponseField>

<RequestExample>

```bash Chile
curl -X POST https://api.enrichlatam.com/v1/tenders \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"rut": "77.261.280-K", "country": "CL"}'
```

```bash Brazil
curl -X POST https://api.enrichlatam.com/v1/tenders \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"rut": "28.591.276/0001-00", "country": "BR"}'
```

```python Python
import requests

r = requests.post(
    "https://api.enrichlatam.com/v1/tenders",
    headers={"X-API-Key": "YOUR_API_KEY"},
    json={"rut": "77.261.280-K", "country": "CL"},
)
print(r.json())
```

</RequestExample>

<ResponseExample>

```json 200 Chile
{
  "rut": "77.261.280-K",
  "total_orders": 3,
  "orders": [
    {
      "code": "1003473-1501-SE26",
      "name": "Compra equipos computacionales",
      "status": "vigente"
    }
  ],
  "source": "mercadopublico",
  "timestamp": "2026-04-05T10:00:00Z"
}
```

```json 200 Brazil
{
  "rut": "28.591.276/0001-00",
  "total_orders": 2,
  "orders": [
    {
      "code": "90001/2026",
      "name": "Aquisição de equipamentos de TI",
      "status": "vigente"
    }
  ],
  "source": "pncp",
  "timestamp": "2026-04-05T10:00:00Z"
}
```

```json 404
{
  "detail": "No tenders found for this organization"
}
```

```json 403
{
  "detail": "Your plan does not include the 'tenders' module. Upgrade at enrichlatam.com."
}
```

</ResponseExample>
